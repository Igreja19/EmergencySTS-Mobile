<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 🌈 FUNDO GRADIENT -->
    <pt.ipleiria.estg.dei.emergencysts.GradientView
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- CONTEÚDO -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:orientation="vertical"
            android:padding="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- ⭐ HEADER MODERNO -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="90dp"
                android:background="#12A84C"
                android:paddingTop="24dp"
                android:paddingHorizontal="16dp"
                android:elevation="6dp">

                <!-- Botão voltar -->
                <ImageView
                    android:id="@+id/btnBack"
                    android:layout_width="30dp"
                    android:layout_height="30dp"
                    android:layout_centerVertical="true"
                    android:src="@drawable/ic_voltaratras"
                    app:tint="#FFFFFF" />

                <!-- Título centrado -->
                <TextView
                    android:id="@+id/tvTitle"
                    android:text="Configuração da API"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:textColor="#FFFFFF"
                    android:layout_centerInParent="true"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content" />

            </RelativeLayout>

            <!-- Ícone com fundo suave -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:layout_marginTop="20dp">

                <ImageView
                    android:layout_width="110dp"
                    android:layout_height="110dp"
                    android:background="@drawable/bg_circle_white"
                    android:padding="24dp"
                    android:src="@drawable/ic_server"
                    app:tint="#12A84C" />
            </LinearLayout>

            <!-- Subtítulo -->
            <TextView
                android:text="Configuração do Servidor"
                android:textStyle="bold"
                android:gravity="center"
                android:textColor="#333"
                android:textSize="17sp"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="12dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <!-- CARD DE CONFIGURAÇÃO -->
            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="4dp"
                android:layout_marginTop="6dp"
                app:cardCornerRadius="20dp"
                app:cardElevation="7dp">

                <LinearLayout
                    android:orientation="vertical"
                    android:padding="20dp"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">

                    <!-- CAMPO URL DO SERVIDOR -->
                    <TextView
                        android:text="URL do Servidor (IP ou domínio)"
                        android:textColor="#444"
                        android:textStyle="bold"
                        android:textSize="15sp"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content" />

                    <EditText
                        android:id="@+id/editServerIp"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:hint="ex.: http://192.168.1.100"
                        android:padding="16dp"
                        android:layout_marginTop="10dp"
                        android:background="@drawable/rounded_input"
                        android:textColor="#000"
                        android:textColorHint="#888" />

                    <!-- CAMPO DO CAMINHO DA API -->
                    <TextView
                        android:text="Caminho da API"
                        android:textColor="#444"
                        android:textStyle="bold"
                        android:textSize="15sp"
                        android:layout_marginTop="20dp"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content" />

                    <EditText
                        android:id="@+id/editApiPath"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:hint="/platf/EmergencySTS/advanced/backend/web/"
                        android:padding="16dp"
                        android:layout_marginTop="10dp"
                        android:background="@drawable/rounded_input"
                        android:textColor="#000"
                        android:textColorHint="#888" />

                    <!-- TEXTO AJUDA -->
                    <TextView
                        android:text="O caminho será anexado automaticamente ao IP.\nExemplo: http://10.0.2.2 + /platf/... = API final"
                        android:textColor="#777"
                        android:textSize="12sp"
                        android:layout_marginTop="8dp"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content" />

                    <!-- BOTÃO RESET -->
                    <Button
                        android:id="@+id/btnReset"
                        android:text="Reset"
                        android:layout_marginTop="24dp"
                        android:background="@drawable/rounded_button_gray"
                        android:textAllCaps="false"
                        android:textStyle="bold"
                        android:textColor="#FFF"
                        android:paddingVertical="16dp"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content" />

                    <!-- BOTÃO GUARDAR -->
                    <Button
                        android:id="@+id/btnSave"
                        android:text="Guardar Configuração"
                        android:layout_marginTop="14dp"
                        android:background="@drawable/rounded_button"
                        android:textAllCaps="false"
                        android:textStyle="bold"
                        android:textColor="#FFF"
                        android:paddingVertical="16dp"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content" />

                </LinearLayout>

            </androidx.cardview.widget.CardView>

        </LinearLayout>
    </ScrollView>

</RelativeLayout>
